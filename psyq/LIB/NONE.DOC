NONE.OBJ の使用法について

NONE.OBJ は Exec() などで呼び出す実行プロセスに
2MB.OBJ などの代わりにリンクして、スタックポインタの
値を正常に受け渡しするためのオブジェクトファイルです。

通常、libsn.lib をリンクすると main()の前処理の段階で
スタックポインタを設定してしまうため、いくら Exec() 
の前段階で処理をしても意味がなくなってしまいます。
もちろん、呼び出した側のプロセスのスタックがどうなって
もかまわないのであればこの限りではありません。

使用方法は　2MB.OBJ と同じ様にリンクしていただくだけです。
なお、RUN.EXE などで実行する予定の実行ファイルにはこの
オブジェクトをリンクするのはおやめください。

				1994.9.12   SCE 開発部

